<div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
        <input type="submit" class="btn btn-primary" name="create" value="{{ lang('button.create') }}" />
        <a href="{{ URL::previous() }}" class="btn btn-default" role="button">{{ lang('button.cancel') }}</a>
    </div>
</div>